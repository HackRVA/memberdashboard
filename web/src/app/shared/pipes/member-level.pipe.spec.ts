import { MemberLevelPipe } from './member-level.pipe';

describe('MemberLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new MemberLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
